import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    body: { flex: 3, alignItems: 'center' },
    header: { backgroundColor: '#0070C0' },
    listItem: { flexDirection: 'column' },
});